define main

function Sqrt(X, Eps: real returns real)
	for initial
		Root := X/2.0
	repeat
		Root := (X /old Root + old Root) / 2.0
	until abs(X - (Root*Root)) < Eps
	returns value of Root
	end for
end function

function main(returns real)
	Sqrt(3.3, 50.0)

end function

